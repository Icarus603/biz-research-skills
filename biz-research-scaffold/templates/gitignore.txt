# Raw data — never commit
data/raw/
*.dta
*.csv
*.xlsx
*.xls
*.sas7bdat
*.parquet
*.feather

# Stata
*.smcl
*.log
~*.dta

# R
.Rhistory
.RData
.Rproj.user/

# Python
__pycache__/
.venv/
*.pyc
.env
dist/

# LaTeX
*.aux
*.bbl
*.blg
*.fls
*.fdb_latexmk
*.synctex.gz
*.out
*.toc

# Regeneratable output
output/logs/

# Literature collection (refs/) — track metadata, exclude large PDFs
# papers.json / manifest.csv / downloaded.json → tracked (small, machine-readable)
# *.pdf → ignored (large binaries, often 200+ MB per project)
refs/*/pdfs/*.pdf
refs/*/pdfs/batch_*.json
refs/*/.pipeline.lock
!refs/*/papers.json
!refs/*/manifest.csv
!refs/*/pdfs/downloaded.json
!refs/*/web/papers.json
!refs/*/web/manifest.csv
!refs/*/search/papers.json
!refs/*/search/manifest.csv
!refs/*/supplement/papers.json
!refs/*/supplement/manifest.csv
# notes/ (lit-scout, deep-read, etc.) tracked by default — markdown, small

# OS
.DS_Store
Thumbs.db
